services:
  MCC:
    image: "localhost/minecraft-console-client:latest"
    volumes:
      - "./data:/opt/data"
    ports:
      - 8043:8043
    networks:
      - server
networks:
  server:
    external: true
    name: server_default
