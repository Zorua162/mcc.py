services:
  robot_mcc_tests:
    build:
      context: .
      dockerfile: Containerfile
    image: robot_mcc_tests
    volumes:
      - "./output:/tests/output"
    networks:
      - server
networks:
  server:
    external: true
    name: server_default
