services:
  robot_mcc_tests:
    build:
      context: .
      dockerfile: Containerfile
    image: zorua162/docker-mcc-py-robot-framework:test
    volumes:
      - "./tests:/tests"
      - "./output:/output"
    networks:
      - server
networks:
  server:
    external: true
    name: server_default
